# Research Log

One row per decision point, not per run. Purpose: answer "what have we tried,
why, and what did we decide" without re-deriving it from scratch or digging
through `weekly/` folders again. See [PUBLICATION_PLAN.md](PUBLICATION_PLAN.md)
for the current open action items this log feeds into.

**Rule going forward**: any experiment that changes a config, a label
definition, a threshold method, or overturns a prior conclusion gets a row
here — even a one-line "tried X, no effect, dropped" row — before starting
the next thing. If it's not here, treat it as not having happened.

| Date | Question / hypothesis | Tried | Result | Decision | Status |
|---|---|---|---|---|---|
| ~W26 (early Jul) | How many reconstructed features does AT-Time need? | `Full-11` vs `Return+Volume` vs `Gap only` vs `Return+Volume+Gap` (recon3) | recon3: PR-AUC 0.455 vs Full-11's 0.239 — nearly doubles | Locked reconstruction features to `log_return_1d,volume_z,gap` | ✅ kept — this is the canonical model (`journey/03`) |
| 2026-07-05 | Where exactly does AT-Time lose to Isolation Forest? | `weekly/2026-W26/contextual_at_misses_vs_if_hits[_no_returnnll]` — pulled the specific 247 contextual test-label days and compared per-case hits/misses | Of 247 labeled events: AT-Time+AT-TimeState+ReturnNLL missed 129 together; AT-Time+AT-TimeState alone missed 171; **Isolation Forest alone correctly flagged 174** | Investigated at case level, raw CSVs saved (`at_all_three_missed_cases.csv`, `if_predicted_label1_cases.csv`) but **no written conclusion or fix** — thread dropped here | ⚠️ unresolved — same gap re-discovered independently in this session's reviewer pass, still open in `PUBLICATION_PLAN.md` item 2 |
| 2026-07-12 | Are we paper-ready? (executive stocktake) | Wrote `weekly/summary_12072026/README.md` + `DETAILS.md` | Explicitly states: PR-AUC gap vs Isolation Forest (0.455 vs 0.653) "must be reported honestly"; lists 5 remaining tasks: lock ground truth, fix label stats to train/val only, peak-extraction ablation, all-111-ticker eval, ≥3 seeds + mean±std, IF comparison under identical protocol | Became the actual to-do list for the next two weeks | ✅ 2 of 5 done (label stats fixed to train-only; peak extraction became the causal detector below) — **3 of 5 still open and now duplicated in `PUBLICATION_PLAN.md` items 2 & 5**: all-ticker eval, multi-seed, IF-under-same-protocol |
| W27 (mid Jul) | Does a global Q99 threshold discard usable score structure? | Shape score (`sharpness = log(1 + score/neighbor mean)`), retrospective, centered window | Q99 shape scoring improved test PR-AUC 0.455→0.514, but uses future neighbors (not causal) | Confirmed shape helps, but retrospective-only — need a causal version | ✅ superseded by causal detector below |
| W27 | Adaptive local peak v1: rolling median/MAD + peak matching, still has Q98 floor | Ran ablation removing the local-z gate but keeping Q98 | **Identical result** with or without the local-z gate | Negative result — improvement was from the Q98 floor + event clustering + ±2-day matching, not from adaptive thresholding. Rejected, rebuilt without any Q floor | ❌ rejected → led directly to the corrected version below (good catch, worth keeping in the paper as a negative result) |
| W27 | Corrected causal local-only detector, no Q anywhere | Causal median/MAD (past-only), local peak height/prominence/width via `scipy.find_peaks` | Test: precision 0.556, recall 0.650, F1 0.599 (weekly numbers, per-split label stats) | Confirms local bumps alone recover far more events than Q99 | ✅ kept, refined further below |
| W27 | Can behavior confirmation cut the local-only detector's false alerts? | Added cooldown + return/volume/gap robust-z confirmation | Test: alerts -34.8%, FP -59.8%, F1 0.599→0.628 (weekly numbers) | Kept as the hybrid causal + behavior detector | ✅ kept, but see next row — numbers changed on promotion |
| 2026-07-2x | Promote W27 detector into the frozen, paper-facing journey | Recomputed with event thresholds from **train-only** stats instead of per-split stats (leakage fix) | Test F1 drops from weekly's 0.628 to **0.606** (the correct, frozen number) | This is `journey/05_causal_local_behavior_detector` now — the current headline result | ✅ frozen — **only ever cite 0.606, never the 0.628 weekly number** |
| 2026-07-27..29 | Reorganize research artifacts out of the git repo | Moved `research_paper/` (1,604 files, 822MB) to `D:\financial-prior-research-paper`, built numbered `journey/00-06` with `MANIFEST.csv` SHA-256 verification | Clean separation of code vs. heavy artifacts | Repo now points to the journey via `research_paper/README.md` | ✅ done, but the July 12 stocktake and the July 5 IF investigation weren't linked from the new journey docs — this is exactly why they had to be rediscovered this session |
| 2026-07-29 (this session) | Rediscovered the same open items as July 12 | Code review pass, baseline capacity/epoch diagnosis, literature check | Confirmed: Isolation Forest still undefeated, baselines in `evaluate_baselines.py` are undersized (toy Transformer/LSTM vs. published Anomaly Transformer config), 25-ticker eval still not expanded, no seed control anywhere | Logged as `PUBLICATION_PLAN.md` items 2, 4, 5 | 🔜 next actions live in `PUBLICATION_PLAN.md`, not here — this log is history, that file is the todo list |
| 2026-07-29 | Is the IF-vs-AT gap just ticker sampling noise, or real? | Ticker-clustered bootstrap (2000 resamples, seed 42) on frozen `03` ranker scores and `05` detector confusion counts, `scripts/bootstrap_confidence.py` | Canonical ranker PR-AUC 95% CI [0.388, 0.530]; IF's point estimate 0.653 falls outside it. Detector F1 95% CI [0.562, 0.653] on the 25-ticker subset | Confirms the IF gap is not sampling noise — sharpens the need to resolve item 2, doesn't soften it | ✅ done → `journey/07_statistical_confidence` |
| 2026-07-29 | Does train (2018-2021) actually resemble test (2023-2024), or has the market regime drifted? | KS test + summary stats on `log_return_1d`/`volume_z`/`gap` across all 3 splits, direct from cached feature CSVs, `scripts/compare_feature_distributions.py` | Train vs. test are fairly close (KS stat 0.02-0.04). **Validation (2022) is actually the outlier** — more different from both train and test (KS 0.08-0.12) than train/test differ from each other. Test (2023-24) is calmer than train, not more volatile as initially guessed | Every config/threshold decision made so far used 2022 as the sole validation year — now confirmed to be an atypical year, not a representative one | ✅ done → `journey/07_statistical_confidence` — strengthens the case for walk-forward, doesn't just theorize it |
| 2026-07-29 | Could we fix the "one weird validation year" problem by scattering random days from 2018-2021 into both train and validation instead of a clean chronological cut? | Discussed randomized/interleaved splitting, then purged+embargo (finance-ML technique for exactly this) | Rejected: model windows are 60 days, and features like `volume_z`/`rolling_vol_*` are themselves rolling calculations — random day-level splits let validation windows overlap 40+/60 days with training windows (or share the rolling-feature lookback), which is leakage regardless of whether the two windows are aligned the same way. Purged+embargo fixes this correctly but burns ~120 days of training data per scattered block and adds real implementation complexity | Walk-forward (chronological, whole-year folds) picked over purged+embargo — same benefit (validation doesn't get stuck on one atypical year), no embargo bookkeeping needed | ✅ decided — this is why `CODE_AUDIT.md`'s "rolling-baseline cold starts at split boundaries" limitation exists: the current code already avoids window overlap at the train/val and val/test boundaries by *not* carrying history across them, at the cost of a cold start each time |
| 2026-07-29 | Add seed control so multi-seed retraining and walk-forward folds aren't confounded by uncontrolled randomness | Added `--seed` (default 0) to `main.py`; seeds `random`/`numpy`/`torch`/`cuda` before `Solver` is built | Weight init, dropout, and DataLoader shuffling are now controlled. `cudnn.benchmark=True` was left on for training speed, so bit-exact reproducibility isn't guaranteed, but that's not what multi-seed comparison needs anyway | Prerequisite for the still-open multi-seed and walk-forward items | ✅ done — `main.py`/`solver.py` |
| 2026-07-29 | Actually retrain (not just analyze) for multi-seed + walk-forward, and plot everything with error bars | Timed 1 epoch first (132s on this RTX 4060 Laptop) to size the job, then launched `scripts/run_walkforward_multiseed.py` in the background: AT-Time x folds {A,B,C} x seeds {0,1,2}, ~2.5h total. Wrote `scripts/plot_walkforward_multiseed.py` (waits on the run) and `scripts/plot_model_comparison.py` (ran immediately — uses existing `01`/`07` data) | Model comparison plot done: `journey/07_statistical_confidence/figures/model_comparison_with_ci.png` — Isolation Forest still clearly ahead, only AT-Time has a CI so far. Walk-forward results pending | Scope kept to AT-Time only for this first real-training pass (not all 9 models x 3 folds x 3 seeds — that's ~7h+, deferred) | 🔄 in progress — check `journey/08_walkforward_multiseed` once the background job finishes |
| 2026-07-30 | First walk-forward x multi-seed run finished (all 9 completed despite a misleading "failed" task-tracking status) — but was it right? | Checked config.json for the new fold C seed 0 run against the frozen canonical `03_canonical_model/data/config.json` before trusting the numbers | **Bug found**: `run_walkforward_multiseed.py`'s `COMMON_ARGS` never set `--label_type`, so all 9 runs silently used `main.py`'s default (`absolute`) instead of the canonical `contextual` label — not the same task at all. This is why fold C's fresh PR-AUC (0.18-0.34 across seeds) looked alarmingly far below the frozen canonical 0.4549 — it wasn't a multi-seed-variance finding, it was an apples-to-oranges bug | Initially archived (renamed, not deleted) rather than overwritten in place; deleted outright shortly after on request, since a wrong-label run has no reuse value and the reason it existed is already captured in this row — no need to keep dead weight around. Added `--label_type contextual` to the script and relaunched all 9 runs fresh | ✅ done — see next row for the real result |
| 2026-07-30 | Corrected walk-forward x multi-seed run (`label_type=contextual` verified in config.json before trusting anything) | 9 real retrains: AT-Time x folds {A: test 2021, B: test 2022, C: test 2023-24 (current)} x seeds {0,1,2} | **Fold C (current canonical setup) PR-AUC across 3 fresh seeds: 0.314, 0.284, 0.419 → mean 0.339 ± 0.071.** The frozen canonical's reported 0.4549 sits above all 3 fresh seeds (closest was 0.419) — plausibly a favorable seed draw, not a typical one. **Event-F1 shows a real regime gap**: fold A (test 2021) 0.145±0.025, fold B (test 2022) 0.162±0.015, fold C (test 2023-24) 0.384±0.078 — much higher in the current test period than in either earlier fold. Confounded with training-set size (folds A/B have 2-3 years of train data vs fold C's 4) — can't yet separate "harder regime" from "less training data" with only this design | The single canonical PR-AUC (0.4549) should not be reported alone in the paper — report the walk-forward mean±std instead, and flag that fold C's event-F1 advantage may partly reflect having 4 years of training data rather than test-period ease | ✅ done → `journey/08_walkforward_multiseed` (`figures/walkforward_multiseed_stability.png`, `data/walkforward_multiseed_metrics.json`) |
| 2026-07-30 | The event-F1 numbers above used solver.py's default `threshold_method=percentile` (Q99) — the naive global threshold journey/04-05 already showed is worse than the real detector. Does the regime gap survive once the actual causal detector is used instead? | Applied the frozen journey/05 causal local-peak + behavior detector (imported directly from `weekly/2026-W27/adaptive_local_spike_experiment.py`, same frozen parameters, no re-searching per fold) to all 9 runs' saved test score timelines via new `scripts/apply_causal_detector_to_folds.py`. Fixed a second latent bug while doing this: the imported module's `add_labels` hardcoded the train cutoff to `2021-12-31` for computing event ±3σ bounds — would have leaked fold A/B's own test years into their "train" reference stats; rewrote with a per-fold train-end parameter | With the real detector: fold A F1 0.432±0.093 (vs Q99's 0.145), fold B 0.353±0.125 (vs Q99's 0.162), fold C 0.585±0.048 (vs Q99's 0.384). **The regime gap is real but much smaller than the Q99 numbers implied** — fold A in particular looked far worse than it actually is under Q99. Fold B has the highest seed-to-seed variance (one seed was a clear outlier at 0.209 vs ~0.425 for the other two) | Always evaluate with the actual frozen detector, never solver.py's default Q99 metrics, when the claim is about detection quality | ✅ done → `journey/08_walkforward_multiseed/figures/q99_vs_causal_detector_by_fold.png`, `data/causal_detector_by_fold_seed.csv` |
| 2026-07-30 | The above still reused ONE frozen parameter set (selected once, on the original canonical run) across all 9 new runs — is that a fair shortcut, or does properly re-selecting parameters per run's own validation change anything? | Wrote `scripts/reselect_causal_detector_per_run.py`: full 324-config local-only grid + 72-config behavior-filter grid, selected on each run's OWN validation scores, evaluated on that run's own test scores — exactly the original method, just looped over all 9 runs (CPU-only, no retraining, ~30 min total) | Behavior-filtered F1: fold A 0.353±0.031 (down from 0.432±0.093 with reused params), fold B 0.379±0.082 (about the same), fold C 0.593±0.061 (about the same). **Every single one of the 9 runs selected `lookback=20`** — the maximum value in the grid — meaning the search never explored whether an even longer lookback helps; grid should be extended before treating 20 as confirmed-optimal. **Fold A's 3 seeds all selected `behavior_min=0.0`** (no behavior filter at all) — plausible cause: fold A's validation year is 2020, the COVID-crash year, where almost every day has unusually large return/volume/gap, so "behavior confirmation" barely discriminates anything and validation prefers to skip it | Reusing frozen params was a reasonable approximation for folds B/C but overstated fold A — the detector's own hyperparameters are themselves regime-sensitive, not just the model's scores. The lookback-at-boundary finding means the grid (10,15,20) should be widened in a future pass | ✅ done → `journey/08_walkforward_multiseed/figures/reused_vs_reselected_params_by_fold.png`, `data/causal_detector_reselected_per_run.csv` |

| 2026-07-31 | Extend local SP500 data (last cached at 2026-03-20) up to today | New `scripts/update_sp500_data.py`: fetches only the missing date range per ticker via `yfinance`, appends to `SP500/*_ohlcv.csv`, then rebuilds each ticker's full feature history using the real `build_stock_features` from `data_factory/data_loader.py` (not reimplemented) | 110/111 tickers now run through 2026-07-30 (latest closed trading day). `BK` failed — Yahoo Finance returns "possibly delisted" for it even on a plain 5-day fetch outside this script, so it's an upstream data-availability issue, not a bug here. Also found and fixed in passing: `SP500_features_vw60_lw60_z5` was just an identical duplicate of `SP500_features_vw60_lw60` (same rows/dates/values, missing one column current code already produces) — both are now kept in sync | Re-run canonical/walk-forward experiments still point at the same frozen historical window (2018-2024), so this extension doesn't change any existing reported number — it's available for future work (e.g. a true out-of-sample check beyond 2024) | ✅ done — investigate `BK` separately if it's needed for something specific |

| 2026-07-31 | Why does Isolation Forest beat AT-Time here when the original Anomaly Transformer paper shows IF as one of the worst baselines? | Read the full paper (arXiv:2110.02642v5, all 20 pages/tables/figures) at `C:\Users\Acer\Documents\cmu\research\2110.02642v5.pdf` | In the paper's Table 1, IF gets F1 53.64-68.62% across 5 datasets vs. the Anomaly Transformer's 92-98% — IF is near the bottom of their baseline table, opposite of our result. Two concrete, paper-cited reasons: (1) the paper uses a **point-adjustment evaluation protocol** (p.6: "if a time point in a...abnormal segment is detected, all anomalies in this...segment are viewed as correctly detected") which structurally favors temporal/sequence models over row-independent methods like IF — our contextual_label evaluation has no such adjustment; (2) the paper's anomalies are **persistent multi-timestep faults** (server/spacecraft/water-treatment sensor failures that last many consecutive readings), which plays to sequence models' strength, whereas our financial anomalies are **single-day point events** — structurally closer to what density-based point methods like IF are built for. Also their anomaly rate (4.2-27.8%, Table 13) is far higher than ours (~0.3-0.4%) | This is real, citable evidence for the paper's discussion section — the IF-beats-AT result isn't necessarily an implementation problem, it may reflect a genuine domain difference (point vs. segment anomalies) between financial daily anomalies and the industrial-fault benchmarks Anomaly Transformer was designed around | ✅ done — cite arXiv:2110.02642v5 Table 1 (p.7), Table 13 (p.19), and the point-adjustment description (p.6) directly |
| 2026-07-31 | Fix baseline fairness for real (PUBLICATION_PLAN.md item 4) | Bumped LSTM-AE (hidden 24→128, 1→2 layers), Transformer-AE (d_model 32→128, 1→2 layers, ffn 64→256), added a new Conv1D-AE baseline, epochs 3→15, and forced `--reconstruction_features log_return_1d,volume_z,gap` + `--label_col contextual_label` to match AT-Time exactly (`scripts/run_baselines_fold_c.py`, fold C, CPU to avoid GPU contention with concurrent AT-TimeState training) | Isolation Forest PR-AUC dropped 0.653→**0.533** once forced onto `contextual_label` (strong evidence the original 01_model_and_baseline_search comparison used a different label for baselines than for AT-Time — a second, previously-unnoticed apples-to-oranges issue beyond the reconstruction-features one). **LSTM-AE/Transformer-AE/1D-CNN-AE are still catastrophic** (PR-AUC 0.016/0.015/0.032) despite the capacity+epoch fix — capacity was not the whole story. Root cause found: total gradient steps are still ~600 (`max_sequence_rows=20000 / batch_size=512 * epochs=15`) vs. AT-Time's own ~22,288 steps (`batch_size=32 * 3184 steps/epoch * 7 epochs`) — a ~37x gap that epoch count alone didn't fix | Capacity fix alone was insufficient — must also close the gradient-step gap (smaller batch and/or more training rows and/or more epochs) before this baseline comparison can be trusted | 🔄 in progress — re-fix and rerun before treating any of these baseline numbers as final |

| 2026-07-31 | AT-Time/AT-TimeState's raw precision-recall curves decay almost linearly (no "hockey stick") — is `score_aggregation=mean` diluting the signal, would `max` sharpen it? | Re-evaluated the already-trained AT-TimeState fold C seed 0 checkpoint (no retraining — copied the checkpoint to a new experiment dir and ran `--mode test --score_aggregation max`) | **Negative result, hypothesis was wrong**: PR-AUC dropped 0.293→0.092, ROC-AUC 0.923→0.826, F1 0.308→0.121, event-F1 0.365→0.170 — worse across every metric, not better | Every ticker-day is scored by up to 60 overlapping windows; `mean` averages out per-window noise, `max` instead picks up the single noisiest window's score for every day (including normal ones), inflating false positives broadly. The "missing context" intuition was right, but the fix isn't aggregation method — it's the causal detector (local peak + behavior confirmation), which is already validated to work. Don't try `score_aggregation=max` again without a different justification | ✅ done — negative result, keep `mean` as the aggregation method |
| 2026-08-10 | Does closing the gradient-step gap (`PUBLICATION_PLAN.md` item 4's remaining action) fix the catastrophic LSTM-AE/Transformer-AE/1D-CNN-AE baselines (PR-AUC ~0.01-0.03)? | Reran `scripts/run_baselines_fold_c.py` with `batch_size=32, epochs=7, max_sequence_rows=110000` (no subsampling — full ~101,887-window fold-C train set), matching AT-Time's own ~22,288 gradient steps exactly, instead of the old `batch_size=512, epochs=15, max_sequence_rows=20000` combo (~585 steps) | **Negative result, hypothesis rejected**: PR-AUC barely moved — LSTM-AE 0.0165 (was 0.0165), Transformer-AE 0.0144 (was 0.0146), 1D-CNN-AE 0.0138 (was 0.032, actually *worse*). Gradient-step count was not the bottleneck | Diagnosed the real cause instead: `fit_sequence_autoencoder`'s `score()` in `scripts/evaluate_baselines.py` does `err.mean(dim=(1, 2))` — collapsing reconstruction error across the **entire 60-day window** (all timesteps + features) into one scalar, then assigns that single diluted number as the score for only the window's last day. A single anomalous day buried among 60 normal ones gets diluted ~60x before it ever reaches the metric. Supporting evidence: the dense `Autoencoder`/`Variational Autoencoder` baselines score single rows directly (no window averaging) and get PR-AUC 0.178/0.190 in the same run, despite far less capacity than the sequence models — capacity isn't what's saving them, the lack of window-dilution is. AT-Time itself never does this: `solver.py`'s `rec_point` keeps per-timestep error, and `aggregate_window_scores` means per-*day* across overlapping windows, never across the other days inside one window | 🔜 real fix identified, not yet applied — rewrite sequence-baseline scoring to use per-timestep (last-position-in-window, or overlapping-window-aggregated like AT-Time) error instead of whole-window mean, then rerun before trusting any LSTM-AE/Transformer-AE/1D-CNN-AE number, and before concluding IF still wins fairly against deep baselines |

| 2026-08-12 | Does penalizing *simultaneous* reconstruction error across `log_return_1d`/`volume_z`/`gap` (rather than summing each independently) sharpen detection? | Added `joint_feature_error()` + `--joint_loss_weight` to `solver.py` (geometric-mean-style pairwise penalty over the reconstruction features), trained several `AT-Time-joint*` variants (`D:\multi-prior-at-run-joint-final`) | **Negative result**: best variant (`AT-Time-joint5-seed0`) scored PR-AUC 0.318, event-F1 0.325 — worse than the canonical model's PR-AUC 0.455, event-F1 0.494. Never beat canonical on any variant tried | Dropping the feature — never logged here before now, caught during a repo cleanup pass. `reconstruction_loss='l1'` (added alongside it, for the same experiment) was also never used in any saved run (`reconstruction_loss` is `"mse"` in every config.json checked) — dropping both together | ❌ rejected — code removed from `solver.py`/`main.py`/`scripts/evaluate_baselines.py`; `tmp/panel_joint/`, `D:\multi-prior-at-run-joint-final` metrics kept as the record if this is ever revisited |
| 2026-08-12 | Reduce `scripts/` clutter — 27 files with no separation between reusable tooling and one-off experiment/analysis scripts | Created `scripts/experiments/`, moved everything except `scripts/evaluate_baselines.py` (the one file other scripts import as a library) into it | Pure reorganization, no behavior change | Every `scripts/foo.py` reference elsewhere in this log (and in `PUBLICATION_PLAN.md`) written before this date now lives at `scripts/experiments/foo.py` — history left as-written, not retroactively edited, matching how the 2026-07-27 `research_paper/` move was logged | ✅ done |

| 2026-08-16 | Why does the new best `AT-State` still hard-miss some contextual events? | Added state-only prior (`--prior_type state`) and ran fold-C seeds 0/1/2 plus feature ablations. Then joined missed-event consensus with cached features to classify missed events by label source and panel-feature extremes. | `AT-State full` is the current best model (PR-AUC 0.675+/-0.040, event-F1 0.686+/-0.027), far above `AT-Time`/`AT-TimeState`. Ablations show return-side state features are the main driver (`no_return` PR-AUC 0.404, event-F1 0.562), while volume helps less (`no_volume` PR-AUC 0.604, event-F1 0.620) and volatility-only preserves some event signal but weak ranking (PR-AUC 0.246, event-F1 0.507). Miss analysis found 351 unique missed events; with `score < 0.5*threshold`, 252 are hard misses and 99 are threshold misses. Among hard misses missed by all 3 seeds (118 events), all satisfy the contextual return condition, but 49 have no 3-sigma panel extreme in log-return/volume/gap under the panel's global test-window std view. Top-score analysis shows the complementary strength: `AT-State` is not merely reacting to large returns; its highest scores concentrate when multiple financial dimensions move together. In the single-run panel data, top-50 score days contain 36 days with return+volume+gap extremes, 12 with two dimensions, only 2 with one dimension, and none with zero. Mean score rises sharply with the number of panel extremes (0 dims: 0.00009; 1 dim: 0.00241; 2 dims: 0.01980; 3 dims: 0.06142). | Report this explicitly: `AT-State` behaves like a detector for multi-dimensional financial shocks more than single-feature spikes. This is a strength for high-confidence shocks, but explains why contextual/single-axis events are missed. The model inputs 11 features, reconstructs 3 (`log_return_1d,volume_z,gap`), and builds the prior from 6 state features; unrelated/non-reconstruction features may pull attention toward a different notion of abnormality. Before adding a learnable projection, run a cleaner `state3` ablation with `--z_state_features log_return_1d,volume_z,gap`. If `state3` reduces hard misses, the current 6-feature state space is too broad; if it hurts, volatility state is genuinely useful and a constrained learnable projection becomes justified. | in progress - report item + next experiment |

| 2026-08-20 | Can sparse/single-feature misses be rescued by calibrated component readout? | Added `--score_type calibrated_components`: compute original reconstruction score plus `log_return_1d`, `volume_z`, and `gap` component errors, percentile-calibrate each channel on validation, then combine by component-wise max. Ran `AT-State full + linear token` fold C seeds 0/1/2. | **Negative result; do not repeat this family.** Mean metrics fell vs `AT-State full + linear token`: PR-AUC 0.7054 -> 0.5864, F1 0.6763 -> 0.5715, recall 0.6557 -> 0.5052, event-F1 0.6942 -> 0.5903, event-recall 0.6759 -> 0.5225. Panels in `tmp/panels/AT-State-LinearToken-CalibratedComponents` show the same failure mode visually: component-wise percentile/max promotes marginal single-feature noise and saturates many scores near 0.99-1.00, damaging ranking. | Lock this as a rejected pattern: do not propose max/OR-style/component-wise shortcut scoring again for sparse anomalies. Future ideas must be context-aware/model-internal or explicitly justified against this failure. | rejected |

| 2026-08-22 | Can we now state that the current best AT-State model beats the non-AT baselines? | Compared `AT-State full + linear token embedding` against the corrected/fair fold-C baseline table (`baselines_fold_c_gradstep_fix/baseline_metrics_fixed.csv`) and the 3-seed AT-State LinearToken summary. | **Yes for the current fair setup.** `AT-State full + linear token` has mean PR-AUC 0.7054, F1 0.6763, recall 0.6557, precision 0.7028, event-F1 0.6942. Corrected Isolation Forest has PR-AUC 0.5327, F1 0.4498, recall 0.3353, precision 0.6829. Other checked baselines are lower: One-Class SVM PR-AUC 0.2360, dense AE 0.1779, VAE 0.1900, LSTM-AE 0.0165, Transformer-AE 0.0144, 1D-CNN-AE 0.0138. | Paper wording should be scoped: "Under the current fold-C contextual-label evaluation, AT-State with linear token embedding outperforms the corrected non-AT baselines, including Isolation Forest." Caveat: sequence AE baselines still have a known window-averaging scoring issue, so do not overclaim that those architectures are intrinsically worse. | kept for paper results |

## Still-unresolved threads (repeated across two stocktakes now)

These three have been "next up" since at least 2026-07-12 and are still open:

1. Evaluate the causal detector on all 111 tickers, not the 25 hand-picked ones.
2. Multi-seed retrain (needs `--seed` added first — still doesn't exist in `main.py`/`solver.py`).
3. Run Isolation Forest through the exact same causal-detector pipeline, same split/label/threshold protocol as AT-Time, so the paper's central "we're better" claim (if any) is measured fairly.

If a future session is tempted to re-investigate "why does Isolation Forest
win" from scratch again: don't — read the 2026-07-05 row above first, the
raw case-level CSVs still exist at
`D:\financial-prior-research-paper\weekly\2026-W26\contextual_at_misses_vs_if_hits*`.
